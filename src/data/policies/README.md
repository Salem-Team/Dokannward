# Removed on purpose
#
# Storefront legal pages must NEVER load HTML from this folder.
# Single source of truth: Admin → Policies → `pages` table → GET /api/pages/{slug}.
# Starter templates (create/restore only) live in admin/resources/policies/.
