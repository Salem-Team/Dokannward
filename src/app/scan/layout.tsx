import type { ReactNode } from "react";

/** Scan routes keep full storefront chrome so cart / checkout stay available. */
export default function ScanLayout({ children }: { children: ReactNode }) {
  return children;
}
