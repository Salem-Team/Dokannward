/** Intentionally empty — fast navigations should never flash a loading screen.
 *  Slow navigations are handled by `RouteLoader` (delayed veil). */
export default function Loading() {
  return null;
}
