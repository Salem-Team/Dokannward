# Production storefront — copy to `.env.production` on the server only.
# Never commit real secrets. Deploy script refuses localhost values.
NEXT_PUBLIC_API_URL=https://dokannward.com/api
REVALIDATE_SECRET=
