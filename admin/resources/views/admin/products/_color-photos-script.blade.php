{{--
    colorPhotosHtml() is provided by the Vite admin bundle (product-media.js).
    Kept as an empty once-block so existing @include calls stay harmless.
--}}
@once
    {{-- no inline script — media pickers boot from admin.js --}}
@endonce
