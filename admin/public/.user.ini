; Applied when the host supports .user.ini (Apache / some shared hosts).
; Local Herd Lite also sets these in php.ini — keep both in sync.
upload_max_filesize = 2048M
post_max_size = 4096M
memory_limit = 1024M
max_file_uploads = 50
max_execution_time = 1800
max_input_time = 1800
