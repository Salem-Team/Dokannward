# Next.js storefront — copy to `.env.local`
# Local admin API (php artisan serve --port=8001)
NEXT_PUBLIC_API_URL=http://localhost:8001/api

# Must match admin REVALIDATE_SECRET so catalog edits purge ISR immediately
REVALIDATE_SECRET=
